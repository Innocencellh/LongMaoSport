package com.live.longmao.activity.person;

import android.os.Bundle;
import android.view.View;

import com.live.longmao.R;
import com.live.longmao.views.BaseActivity;

/**
 * Created by HPDN on 2017/1/14.
 */
public class PersonEditPhoneActivity extends BaseActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setView(R.layout.activity_person_edit_phone);
        setTitle("更换手机号");
    }

    @Override
    public void onClick(View v) {
        super.onClick(v);
        switch (v.getId()){

        }
    }
}
